{:tasks
 {test {:doc "Run the complete standalone mizuho suite"
        :task (shell "clojure -M -m mizuho.test-runner")}
  audit {:doc "Validate canonical EDN and artifact boundaries"
         :task (shell "clojure -M scripts/audit.clj")}}}
